export interface TColors {
  gray: string;
  black: string;
  red: string;
  grayLighter: string;
}

export interface TDevices {
  desktop: string;
  tablet: string;
  mobile: string;
}

export interface TFonts {
  initialFontSize: number;
  normal: number;
  bold: number;
}
